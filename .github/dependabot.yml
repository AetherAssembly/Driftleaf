version: 2
updates:
  # npm dependencies
  - package-ecosystem: npm
    directory: "/"
    schedule:
      interval: weekly
      day: monday
      time: "03:00"
    # Group dependency updates for better PR organization
    groups:
      production-dependencies:
        dependency-type:
          production      
      development-dependencies:
        dependency-type:
          development
